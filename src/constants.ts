export enum TotalSteps {
  CONNECT_WALLET = "ConnectWallet",
}

export enum Wallets {
  Phantom = "Phantom",
  Metamask = "MetaMask",
  Backpack = "Backpack",
  Rabby = "Rabby Wallet",
}

export enum ERROR_CODE {
  BadRequest = 400,
}
